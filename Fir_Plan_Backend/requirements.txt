pip install fastapi uvicorn sqlalchemy pymysql passlib[bcrypt] python-jose

app/
 ├── main.py                # App bootstrap & router registration
 ├── database.py            # MySQL engine + session
 ├── config.py              # Env vars, secrets, DB URL
 │
 ├── models/                # SQLAlchemy models
 │    ├── user.py
 │    ├── plan.py
 │    ├── subscription.py
 │    ├── follower.py
 │    └── __init__.py
 │
 ├── schemas/               # Pydantic request/response schemas
 │    ├── auth.py
 │    ├── user.py
 │    ├── plan.py
 │    └── __init__.py
 │
 ├── auth/                  # Authentication & authorization
 │    ├── jwt.py            # Token create/verify
 │    ├── dependencies.py  # get_current_user, role guards
 │    └── password.py      # hash & verify passwords
 │
 ├── routes/                # API endpoints
 │    ├── auth.py           # signup/login
 │    ├── plans.py          # plan CRUD + subscribe
 │    ├── users.py          # follow/unfollow + feed
 │    └── __init__.py
 │
 ├── utils/                 # Small helpers (optional)
 │    └── response.py
 │
 └── __init__.py
